# Pre-commit hooks configuration.
#
# Runs black (formatting) automatically before each commit so that
# style issues are caught locally before they reach CI.

repos:
  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black
        language_version: python3
